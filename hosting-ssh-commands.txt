# SOLUSI 1: Symbolic Link (PALING DIREKOMENDASIKAN)
# Jalankan via SSH di hosting:

cd /var/www/finwa_web_id_usr/data/www/finwa.web.id/public
ln -s ../storage/app/public storage

# Atau jika sudah ada folder storage, hapus dulu:
rm -rf storage
ln -s ../storage/app/public storage

# Verifikasi:
ls -la storage
# Seharusnya muncul: storage -> ../storage/app/public


# SOLUSI 2: Copy File (Jika symbolic link tidak bisa)
# Jalankan via SSH:

cd /var/www/finwa_web_id_usr/data/www/finwa.web.id
cp -r storage/app/public/* public/storage/

# CATATAN: Setiap kali upload avatar baru, harus copy lagi!


# SOLUSI 3: Ubah Konfigurasi Laravel (PERMANENT FIX)
# Tidak perlu symbolic link sama sekali
# Lihat file: solution-3-change-storage-path.txt
